
#include <stdio.h>

int main() {
    int arr[100],i,n,big,small;
    printf("Enter Number Of Elements : ");
    scanf("%d", &n);
    
    printf("Enter Elements:\n");
    for ( i = 0; i < n; i++) 
    {
        scanf("%d", &arr[i]);
    }
    big=small=arr[0];
    for ( i = 1; i < n; i++)
    {
    if(arr[i]>big){
        big=arr[i];
    }else{
        small=arr[i];
    }
}
printf("Biggest Element is : %d \n",big);
printf("Smallest Element is : %d \n",small);
    return 0;
}

OUTPUT:
Enter Number Of Elements : 2
Enter Elements:
1
2
Biggest Element is : 2 
Smallest Element is : 1 
